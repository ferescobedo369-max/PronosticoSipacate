name: Actualizacion Pronostico ICC
on:
  schedule:
    - cron: '0 12 * * *' # 6:00 AM Guatemala
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar codigo
        uses: actions/checkout@v3
      
      - name: Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Instalar Librerias
        run: pip install -r requirements.txt
          
      - name: Generar Datos CSV
        # Nombre corregido según tus archivos en GitHub
        run: python 1_Median_and_average_conditions.py
        
      - name: Generar Mapas y Graficas
        # Nombre corregido según tus archivos en GitHub
        run: python 3_Map_200.py

      - name: Guardar resultados en el repositorio
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add .
          git commit -m "Actualización automática de pronóstico" || exit 0
          git push
